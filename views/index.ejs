<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Turnos Ciudadanos</title>
  <style>
    body { font-family: Arial; padding: 20px; background: #f5f5f5; }
    h1, h2 { color: #333; }
    form { margin-bottom: 30px; background: #fff; padding: 20px; border-radius: 8px; }
    label, input { display: block; margin-bottom: 10px; }
    input { padding: 10px; width: 100%; }
    button { padding: 10px 16px; background: #2e86de; color: white; border: none; cursor: pointer; }
    table { width: 100%; border-collapse: collapse; background: white; margin-top: 20px; }
    th, td { padding: 12px; border: 1px solid #ccc; }
    .total { margin-top: 20px; font-weight: bold; background: #ffeeba; padding: 10px; border-left: 4px solid #ffc107; }
  </style>
</head>
<body>

  <h1>Agencia de Atención a Ciudadanos</h1>

  <h2>Registrar nueva solicitud</h2>
  <form method="POST" action="/agregar">
    <label>Nombre completo:</label>
    <input type="text" name="nombre" required>

    <label>Tiempo estimado (1 a 120 minutos):</label>
    <input type="number" name="tiempo" min="1" max="120" required>

    <button type="submit">Agregar turno</button>
  </form>

  <h2>Orden de Atención</h2>
  <table>
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Tiempo Estimado</th>
        <th>Tiempo de Espera</th>
      </tr>
    </thead>
    <tbody>
      <% turnos.forEach(turno => { %>
        <tr>
          <td><%= turno.nombre %></td>
          <td><%= turno.tiempo %> min</td>
          <td><%= turno.espera %> min</td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <div class="total">
    Nivel total de inconformidad: <%= inconformidad_total %> minutos
  </div>

</body>
</html>